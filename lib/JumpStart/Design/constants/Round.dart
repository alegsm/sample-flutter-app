import 'package:appfolio/JumpStart/Design/constants/JumpStartConstants.dart';
import 'package:flutter/material.dart';


class Round
{
  static const BorderRadius defaultRadius =  const BorderRadius.all(const Radius.circular(20.0));
  static const BoxDecoration defaultBoxDecoration = const BoxDecoration(border: JumpStartConstant.defaultBorder, borderRadius: defaultRadius);
}